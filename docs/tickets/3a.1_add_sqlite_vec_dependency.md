# Ticket 3a.1: Add sqlite-vec dependency + load extension

## Summary
Add sqlite-vec Go dependency and ensure the extension is loaded when SQLite connection is opened.

## Context
sqlite-vec is a vector search SQLite extension. We need to add it as a dependency and ensure it's loaded when our SQLite connection is initialized. This is the foundation for all vector operations.

## Acceptance Criteria
- [ ] `github.com/asg017/sqlite-vec-go-bindings/cgo` added to go.mod
- [ ] Extension is loaded when SQLite connection opens
- [ ] No CGO errors on build
- [ ] Existing tests still pass

## Technical Details
- Use CGO bindings: `github.com/asg017/sqlite-vec-go-bindings/cgo`
- Import in store package: `import _ "github.com/asg017/sqlite-vec-go-bindings/cgo"`
- Ensure CGO_ENABLED=1 in build pipeline
- Test with `go test ./internal/infrastructure/store/...`

## Files
- `go.mod`
- `go.sum`
- `internal/infrastructure/store/sqlite_vec.go` (new)
