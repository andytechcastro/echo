# Ticket 3a.4: Bundle all-MiniLM-L6-v2 ONNX model (22MB)

## Summary
Download and bundle the all-MiniLM-L6-v2 ONNX model file, and implement automatic download on first run.

## Context
The ONNX model needs to be available locally. We should download it automatically on first run if not present, with a fallback to a bundled version.

## Acceptance Criteria
- [ ] Model downloaded automatically on first run
- [ ] Model stored in `~/.config/echo/models/all-MiniLM-L6-v2.onnx`
- [ ] Checksum verification after download
- [ ] Fallback error message if download fails
- [ ] `--model-path` flag to override default location

## Technical Details
- Download URL: Hugging Face (onnx-models/all-MiniLM-L6-v2-onnx)
- Expected checksum: SHA256 of the .onnx file
- Default path: `~/.config/echo/models/all-MiniLM-L6-v2.onnx`
- CLI flag: `--model-path /path/to/model.onnx`

```bash
# First run output:
📦 Downloading embedding model (22MB)...
✅ Model downloaded and verified (sha256: abc123...)
```

## Files
- `internal/infrastructure/embedder/model_download.go` (new)
- `cmd/echo/main.go` (add --model-path flag)
- `internal/config/config.go` (add ModelPath field)
